# 此刻项目强制开发规范

本文件适用于 `Now-server` 全部目录，优先级高于项目中的一般性说明。后续任何人工或 AI 代码修改都必须遵守。

## 1. 禁止业务魔法值

- Java 生产代码不得直接散落业务状态、模式、来源、类型、限制数量、分页边界、有效期、算法名称、错误文案等魔法值。
- 业务魔法值必须放入语义明确的枚举类；Jakarta Validation 等要求编译期常量的场景，也必须把常量定义在枚举类中统一引用。
- HTTP 状态使用框架提供的 `HttpStatus` 枚举；数据库状态必须从领域枚举绑定为 SQL 参数，不得直接写在 SQL 字符串中。
- 表名、列名、SQL 关键字、空值、零值以及数学公式中不可消除的结构值不视为业务魔法值，但公式参数必须枚举化并写明计算口径。
- 新增业务值前必须先检查已有枚举，禁止在不同类中重复定义同一含义。

## 2. 动作分类必须数据库驱动

- 一级动作分类存储在 `exercise_category`，二级动作分类存储在 `exercise_subcategory`。
- 动作与二级分类的多对多关系存储在 `exercise_subcategory_mapping`。
- 不得在 Java、Vue、JavaScript 或接口 DTO 中按胸、背、肩等部位创建固定二级分类字段、固定标签数组或分类判断分支。
- 动作查询统一使用通用 `category` 和 `subcategory` 参数；接口统一返回动态 `subcategories` 集合。
- 新增、停用、排序或更名分类应通过 Flyway 迁移或后台数据管理完成，不得要求修改查询代码。
- 禁止恢复 `chest_regions`、`back_regions` 等按部位拆分的 JSON 列，禁止维护数据库与代码两套分类来源。

## 3. 注释与文档覆盖率

- 所有新增或修改的方法、构造器及具有行为的方法体都必须有紧邻声明的 Javadoc 或解释性注释。
- 注释必须说明职责；复杂方法还必须说明参数、返回值、事务边界、权限边界、计算口径或异常条件，禁止使用“处理数据”之类无信息量注释凑数。
- `src/main/java` 的注释行数占非空源码行数比例不得低于 30%。比例计算包含 `//`、`/* ... */` 和 Javadoc 注释行，不包含空行。
- Maven 测试必须包含并通过代码规范门禁；任何导致方法注释缺失或总注释率低于 30% 的修改不得交付。
- 数据库迁移和对外接口变更必须同步更新 `PROJECT_CONTEXT.md`，写明当前状态、验证结果和下一步。

## 4. 后端验证与运行规则

- 后端代码或 Flyway 迁移变更后必须运行全量 Maven 测试和构建。
- 验证通过后必须自动重启当前后端，不再等待用户确认。
- 重启后必须实际验证 `/api/v1/health` 和本次修改涉及的接口，不能把“源码已修改”当作“运行态已更新”。
- 数据库凭据只能通过进程环境变量使用，禁止写入源码、文档、Git、日志或命令输出。
